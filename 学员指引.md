# 航海家伴学 Agent · 学员安装使用指引

> 这是一个「资料库伴学智能体」skill：**内置楚川/叁斤/包子三讲师知识库（K1–K16），装上即可直接答疑**；同时也是一套可复用的搭建方法论，你可以照它搭自己的 Agent。
>
> **零基础先看这句**：本 skill 就是一个普通文件夹，里面是 Markdown 文档。下载它 → 放到你 AI 工具的技能目录（或直接把文件夹丢进对话）→ 完事。下面所有步骤都围绕「下载」和「放对位置」两件事。

## 一、下载（二选一）

**方式 1 · 鼠标党（推荐，无需任何命令）**
1. 浏览器打开：https://github.com/lauzhegng4-coder/banxue-agent
2. 点绿色 **`<> Code`** 按钮 → **Download ZIP**
3. 解压，得到 `banxue-agent-main` 文件夹（可改名 `banxue-agent`）

**方式 2 · 命令行**
```bash
git clone https://github.com/lauzhegng4-coder/banxue-agent.git
```

## 二、装到哪（按你用的 AI 工具选）

**通用兜底（对所有人都有效，先试这个）**：
把你下载的文件夹直接拖进 AI 工具的对话窗口，说一句：
> 读一下 SKILL.md，按里面的规则开始。

AI 能读文件就能用，不挑工具。

**想装成常驻技能**（每次自动可用，不用反复拖）：

| 你在用 | 怎么放 | 怎么放（命令行版） |
|---|---|---|
| WorkBuddy | 把文件夹拷到 `~/.workbuddy/skills/` 下，重启 WorkBuddy | `rsync -a --exclude '.git' banxue-agent/ ~/.workbuddy/skills/banxue-agent/` |
| Claude Code | 拷到 `~/.claude/skills/` 下 | `rsync -a --exclude '.git' banxue-agent/ ~/.claude/skills/banxue-agent/` |
| Codex / 其他 CLI | 文件夹就是工作目录，重开对话 | 同左 |
| Cursor / 编辑器 | 用编辑器打开这个文件夹，让 AI 读 SKILL.md | 同左 |
| 飞书 bot | 什么都不用装：找 Jack 加可用范围 → 私聊机器人 → 发配对码 → Jack 审批 | — |

Mac 找 `~/.workbuddy` 这类隐藏文件夹：访达按 `Cmd+Shift+G`，输入路径回车。

## 三、怎么用（三种路径，按目标选）

### A. 直接当伴学 bot 用（装完即用）
- 激活 skill（WorkBuddy 里说「伴学」；其他工具用「通用兜底」那句）。
- 首次激活会出开场白，列三讲师板块，选一个就能开讲：
  - **楚川**：AI 内容工程化（知识库三层/选词/脚本/流水线）、IP 访谈、竞品调研
  - **叁斤**：知识库 → 内容 Agent（六大库/采访机器人/录音处理/朋友圈文案）、人群洞察、工作台
  - **包子**：成为 AI 的伯乐 / AI Native（四根缰绳/视频生产重塑/AI 新品类）
- 回答都会标「依据 K几 第几节」，可回原文核对。

### B. 学方法，自己搭一个
1. 打开 `SKILL.md`，照「四层核心资产」搭你自己的 Agent：
   - **知识库** —— 资料编 K 号、写 `KNOWLEDGE.md` 总索引
   - **伴学 SOP** —— 抄 `AGENTS.md` 模板，按定档→路线图→陪跑→复盘改
   - **学员画像** —— `profiles/` 一人一档
   - **喂养通道** —— 新资料持续入库并更新索引
2. 想跑在飞书里：照 SKILL.md 末尾「通道落地」章节（Hermes + 飞书自建应用 + 长连接）

### C. 直接用 Jack 已搭好的飞书 bot
- 见上表「飞书 bot」行。

## 四、后续更新

**鼠标党**：GitHub 页面重新 Download ZIP → 解压 → 删掉旧的 `banxue-agent` 文件夹 → 新的放回原位置。一分钟搞定。

**命令行**：
```bash
cd banxue-agent && git pull
# WorkBuddy 用户再同步一次：
rsync -a --delete --exclude '.git' ./ ~/.workbuddy/skills/banxue-agent/
```

**飞书 bot 用户**：什么都不用做，Jack 更新后你自动用新版。

## 常见问题

- **装完 AI 没识别？** 确认对应路径下有 `SKILL.md`（WorkBuddy：`~/.workbuddy/skills/banxue-agent/SKILL.md`），重启工具。
- **这个 skill 能直接回答课程问题吗？** 能。knowledge/ 已内置 K1–K16 全量资料（讲师讲义/PPT/逐字稿提纯），直接问即可。
- **不想装，只想问一次？** 用「通用兜底」：拖文件夹进对话。
- **使用有限制吗？** 无限制。任何能读本地文件的 AI 工具都能用。
