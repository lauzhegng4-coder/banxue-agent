# 航海家伴学 Agent · 学员安装使用指引

> 这是一个「资料库伴学智能体」skill：**内置楚川/叁斤/包子三讲师知识库（K1–K16），装上即可直接答疑**；同时也是一套可复用的搭建方法论，你可以照它搭自己的 Agent。

## 一、clone 仓库

```bash
git clone https://github.com/lauzhegng4-coder/banxue-agent.git
```

## 二、安装到 WorkBuddy

```bash
rsync -a --exclude '.git' banxue-agent/ ~/.workbuddy/skills/banxue-agent/
```

装好后**重启/刷新一次 WorkBuddy**，skill 自动被识别。
确认路径：`~/.workbuddy/skills/banxue-agent/SKILL.md` 存在即可。

## 三、怎么用（两种路径，按目标选）

### A. 直接当伴学 bot 用（装完即用）
- 对 WorkBuddy 说「伴学」或任意课程问题，激活本 skill。
- 首次激活会出开场白，列三讲师板块，选一个就能开讲：
  - **楚川**：AI 内容工程化（知识库三层/选词/脚本/流水线）、IP 访谈、竞品调研
  - **叁斤**：知识库 → 内容 Agent（六大库/采访机器人/录音处理/朋友圈文案）、人群洞察、工作台
  - **包子**：成为 AI 的伯乐 / AI Native（四根缰绳/视频生产重塑/AI 新品类）
- 回答都会标「依据 K几 第几节」，可回原文核对。

### B. 学方法，自己搭一个
1. 打开 `~/.workbuddy/skills/banxue-agent/SKILL.md`
2. 照「四层核心资产」搭你自己的 Agent：
   - **知识库** —— 资料编 K 号、写 `KNOWLEDGE.md` 总索引
   - **伴学 SOP** —— 抄 `AGENTS.md` 模板，按定档→路线图→陪跑→复盘改
   - **学员画像** —— `profiles/` 一人一档
   - **喂养通道** —— 新资料持续入库并更新索引
3. 想跑在飞书里：照 SKILL.md 末尾「通道落地」章节（Hermes + 飞书自建应用 + 长连接）

### C. 直接用 Jack 已搭好的飞书 bot
- 不用装 skill。让 Jack 在飞书应用把你的账号加入「可用范围」→ 你私聊机器人 → 按提示发配对码 → Jack 审批 → 即可对话。

## 四、后续更新

仓库有改动时，拉取再覆盖即可：

```bash
cd banxue-agent && git pull
rsync -a --delete --exclude '.git' ./ ~/.workbuddy/skills/banxue-agent/
```

更新后重启一次 WorkBuddy 生效。

## 常见问题

- **装完 WorkBuddy 没识别？** 确认路径是 `~/.workbuddy/skills/banxue-agent/SKILL.md`，并重启 WorkBuddy。
- **这个 skill 能直接回答课程问题吗？** 能。knowledge/ 已内置 K1–K16 全量资料（讲师讲义/PPT/逐字稿提纯），直接问即可。
- **使用有限制吗？** 无限制。任意终端、任意智能体框架（WorkBuddy / Hermes / 其他）都能用。
