# 航海家伴学 Agent · 学员安装使用指引

> 这是一个「资料库伴学智能体」skill：**内置楚川/叁斤/包子三讲师知识库（K1–K16），装上即可直接答疑**；同时也是一套可复用的搭建方法论，你可以照它搭自己的 Agent。

## 一、clone 仓库

```bash
git clone https://github.com/lauzhegng4-coder/banxue-agent.git
```

## 二、安装（按你用的 Agent 选）

**前提**：本 skill 是纯文件包（SKILL.md + AGENTS.md + knowledge/），不含任何框架专属代码，任何能读本地文件的 AI 编码/对话 Agent 都能用。

| 你在用 | 怎么装 |
|---|---|
| WorkBuddy | `rsync -a --exclude '.git' banxue-agent/ ~/.workbuddy/skills/banxue-agent/` → 重启 WorkBuddy |
| Claude Code | `mkdir -p ~/.claude/skills && rsync -a --exclude '.git' banxue-agent/ ~/.claude/skills/banxue-agent/` |
| Codex / 其他 CLI Agent | 把仓库放进 Agent 的工作目录（或配置的 skill/规则目录），对话里让它读 `SKILL.md` |
| Cursor / 任意编辑器 Agent | 用 VSCode/Cursor 打开仓库文件夹，让 Agent 读 `SKILL.md` 后按 AGENTS.md 的 SOP 干活 |
| 飞书 bot | 不用装。让 Jack 把你的账号加入飞书应用「可用范围」→ 私聊机器人 → 发配对码 → Jack 审批即可 |

**通用兜底**（上面都不沾边时）：把仓库文件夹拖进你 Agent 的对话窗口，说「读 SKILL.md，按里面的规则开始」。

## 三、怎么用（三种路径，按目标选）

### A. 直接当伴学 bot 用（装完即用）
- 激活 skill（WorkBuddy 里说「伴学」；其他 Agent 按「通用兜底」方式）。
- 首次激活会出开场白，列三讲师板块，选一个就能开讲：
  - **楚川**：AI 内容工程化（知识库三层/选词/脚本/流水线）、IP 访谈、竞品调研
  - **叁斤**：知识库 → 内容 Agent（六大库/采访机器人/录音处理/朋友圈文案）、人群洞察、工作台
  - **包子**：成为 AI 的伯乐 / AI Native（四根缰绳/视频生产重塑/AI 新品类）
- 回答都会标「依据 K几 第几节」，可回原文核对。

### B. 学方法，自己搭一个
1. 打开 `SKILL.md`，照「四层核心资产」搭你自己的 Agent：
   - **知识库** —— 资料编 K 号、写 `KNOWLEDGE.md` 总索引
   - **伴学 SOP** —— 抄 `AGENTS.md` 模板，按定档→路线图→陪跑→复盘改
   - **学员画像** —— `profiles/` 一人一档
   - **喂养通道** —— 新资料持续入库并更新索引
2. 想跑在飞书里：照 SKILL.md 末尾「通道落地」章节（Hermes + 飞书自建应用 + 长连接）

### C. 直接用 Jack 已搭好的飞书 bot
- 见上表「飞书 bot」行，配对审批后即可对话。

## 四、后续更新

```bash
cd banxue-agent && git pull
```

然后把仓库重新同步到你装的位置（同第二节命令，`rsync` 加 `--delete`），再重启/重开一次 Agent 生效。

## 常见问题

- **装完 Agent 没识别？** 确认对应路径下 `SKILL.md` 存在（WorkBuddy：`~/.workbuddy/skills/banxue-agent/SKILL.md`），并重启 Agent。
- **这个 skill 能直接回答课程问题吗？** 能。knowledge/ 已内置 K1–K16 全量资料（讲师讲义/PPT/逐字稿提纯），直接问即可。
- **使用有限制吗？** 无限制。任何能读本地文件的 Agent 都能用。
