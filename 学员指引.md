# 航海家伴学 Agent · 学员安装使用指引

> 这是一个「资料库伴学智能体」的**搭建方法论** skill：教你怎么把一堆学习资料（讲义/群精华/逐字稿）变成一个会教人、懂学员的 AI。它不是直接能跑的 bot，而是一套可复用的做法。

## 一、clone 仓库

```bash
git clone https://github.com/lauzhegng4-coder/feishu-companion-agent.git
```

## 二、安装到 WorkBuddy

```bash
cp -r feishu-companion-agent ~/.workbuddy/skills/feishu-companion-agent
```

装好后**重启/刷新一次 WorkBuddy**，skill 自动被识别。
确认路径：`~/.workbuddy/skills/feishu-companion-agent/SKILL.md` 存在即可。

## 三、怎么用（两种路径，按目标选）

### A. 学方法，自己搭一个（推荐）
1. 打开 `~/.workbuddy/skills/feishu-companion-agent/SKILL.md`
2. 照「四层核心资产」搭你自己的 Agent：
   - **知识库** —— 资料编 K 号、写 `KNOWLEDGE.md` 总索引
   - **伴学 SOP** —— 抄 `AGENTS.md` 模板，按定档→路线图→陪跑→复盘改
   - **学员画像** —— `profiles/` 一人一档
   - **喂养通道** —— 新资料持续入库并更新索引
3. 想跑在飞书里：照 SKILL.md 末尾「通道落地」章节（Hermes + 飞书自建应用 + 长连接）

### B. 直接用 Jack 已搭好的 bot
- 不用装 skill。那是跑在 Jack 飞书里的**同一个**机器人。
- 让 Jack 在飞书应用把你的账号加入「可用范围」→ 你私聊机器人 → 按提示发配对码 → Jack 审批 → 即可对话。

## 四、后续更新

仓库有改动时，拉取再覆盖即可：

```bash
cd feishu-companion-agent && git pull
cp -r . ~/.workbuddy/skills/feishu-companion-agent
```

## 常见问题

- **装完 WorkBuddy 没识别？** 确认路径是 `~/.workbuddy/skills/feishu-companion-agent/SKILL.md`，并重启 WorkBuddy。
- **这个 skill 能直接回答问题吗？** 不能，它是方法论。真正答疑的是你按它搭出来的 Agent。
- **使用有限制吗？** 无限制。任意终端、任意智能体框架（WorkBuddy / Hermes / 其他）都能用，按你自己的资料搭 Agent 即可。
